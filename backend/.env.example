APP_ENV=development
LOG_LEVEL=INFO
CORS_ORIGINS=http://localhost:5173

# gemini | mock
LLM_PROVIDER=mock

GEMINI_API_KEY=
GEMINI_MODEL=gemini-1.5-flash

SESSION_TTL_MINUTES=60
AUDIT_LOG_PATH=logs/audit.log

# Token bearer requerido en el header Authorization para /chat, /triage y /atencion.
# Genera uno propio, por ejemplo: python -c "import secrets; print(secrets.token_hex(32))"
API_TOKEN=
